<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="google-site-verification" content="6UhcIl_FY6btNpMlR20k54CX018T0cjAm0dZsPJoDJA" />
    <meta name="description" content="Plateforme de shotgun pour les ALLO de Seker Story a l IMT Atlantique Brest. Reserve des services limites publies par la liste BDE." />
    <meta name="keywords" content="ALLO, BDE, Seker Story, IMT Atlantique, Brest, shotgun, services, etudiants" />
    <meta property="og:title" content="ALLO Seker - Shotgun BDE IMT Atlantique Brest" />
    <meta property="og:description" content="Plateforme de shotgun pour les ALLO de Seker Story a l IMT Atlantique Brest. Reserve des services limites publies par la liste BDE." />
    <meta property="og:image" content="/images/logo.png" />
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content="summary" />
    <link rel="icon" type="image/png" href="/images/logo.png" />
    <title>Inscription BDE - ALLO Seker</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="/css/style.css">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>* { font-family: 'Inter', sans-serif; }</style>
</head>
<body class="min-h-screen">
    <nav class="navbar py-4">
        <div class="max-w-6xl mx-auto px-4 sm:px-6">
            <div class="flex justify-between items-center">
                <a href="/live" class="text-gray-400 hover:text-white text-sm transition-colors">Live</a>
                <a href="/">
                    <img src="/images/logo.png" alt="ALLO Seker" class="h-16 md:h-20">
                </a>
                <a href="/mes-allos" class="text-gray-400 hover:text-white text-sm transition-colors">Mes ALLO</a>
            </div>
        </div>
    </nav>

    <main class="max-w-md mx-auto px-4 sm:px-6 py-8">
        <div class="card rounded-2xl shadow-xl overflow-hidden">
            <div class="section-header p-6 md:p-8 text-center">
                <h1 class="text-2xl font-bold text-white">Inscription BDE</h1>
                <p class="text-white/70 text-sm mt-1">Rejoins l'equipe</p>
            </div>

            <div class="p-6 md:p-8">
                <% if (error) { %>
                    <div class="bg-red-500/10 border border-red-500/20 text-red-400 p-4 rounded-lg mb-6">
                        <%= error %>
                    </div>
                <% } %>

                <form action="/auth/register" method="POST" class="space-y-4">
                    <div class="grid grid-cols-2 gap-4">
                        <div>
                            <label for="firstName" class="block text-sm font-medium text-gray-300 mb-2">Prenom *</label>
                            <input type="text" id="firstName" name="firstName" required placeholder="Prenom"
                                   class="input w-full px-4 py-3 rounded-xl">
                        </div>
                        <div>
                            <label for="lastName" class="block text-sm font-medium text-gray-300 mb-2">Nom *</label>
                            <input type="text" id="lastName" name="lastName" required placeholder="Nom"
                                   class="input w-full px-4 py-3 rounded-xl">
                        </div>
                    </div>

                    <div>
                        <label for="email" class="block text-sm font-medium text-gray-300 mb-2">Email *</label>
                        <input type="email" id="email" name="email" required placeholder="ton@email.com"
                               class="input w-full px-4 py-3 rounded-xl">
                    </div>

                    <div>
                        <label for="phone" class="block text-sm font-medium text-gray-300 mb-2">Telephone</label>
                        <input type="tel" id="phone" name="phone" placeholder="06 XX XX XX XX"
                               class="input w-full px-4 py-3 rounded-xl">
                    </div>

                    <div>
                        <label for="bdeListId" class="block text-sm font-medium text-gray-300 mb-2">Liste BDE *</label>
                        <select id="bdeListId" name="bdeListId" required
                                class="input w-full px-4 py-3 rounded-xl">
                            <option value="">Selectionner une liste</option>
                            <% bdeLists.forEach(list => { %>
                                <option value="<%= list.id %>"><%= list.name %><%= list.school ? ' - ' + list.school : '' %></option>
                            <% }) %>
                        </select>
                    </div>

                    <div>
                        <label for="password" class="block text-sm font-medium text-gray-300 mb-2">Mot de passe *</label>
                        <input type="password" id="password" name="password" required minlength="6" placeholder="********"
                               class="input w-full px-4 py-3 rounded-xl">
                        <p class="text-xs text-gray-500 mt-1">Minimum 6 caracteres</p>
                    </div>

                    <div>
                        <label for="confirmPassword" class="block text-sm font-medium text-gray-300 mb-2">Confirmer *</label>
                        <input type="password" id="confirmPassword" name="confirmPassword" required minlength="6" placeholder="********"
                               class="input w-full px-4 py-3 rounded-xl">
                    </div>

                    <button type="submit"
                            class="btn-shotgun w-full text-white font-semibold py-3 rounded-xl mt-2">
                        S'inscrire
                    </button>
                </form>

                <p class="text-center text-gray-400 mt-6">
                    Deja un compte ?
                    <a href="/auth/login" class="text-pink-400 hover:text-pink-300 font-semibold">Se connecter</a>
                </p>
            </div>
        </div>
    </main>

    <footer class="footer-dark text-gray-400 py-6 mt-12">
        <div class="max-w-6xl mx-auto px-4 sm:px-6 text-center">
            <img src="/images/logo.png" alt="ALLO Seker" class="h-10 mx-auto mb-3 opacity-70">
            <p class="text-sm text-gray-500">Developed by Wendy Technology</p>
        </div>
    </footer>
</body>
</html>
